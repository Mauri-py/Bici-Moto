import 'package:flutter/material.dart';

// Colores generales
const Color colorFondoPrincipal = Colors.black; // Fondo principal de toda la app
const Color colorTextoPrincipal = Colors.white; // Texto principal
const Color colorTextoSecundario = Colors.white30; // Texto secundario (menos importante)
const Color colorAdvertencia = Colors.red; // Indicadores de error o advertencia

// Colores específicos de componentes en dashboard.dart
const Color colorVelocimetroFondo = Color(0xFF6B4339); // Fondo del velocímetro
const Color colorVelocimetroAro = Color(0xFF52342C); // Aro del velocímetro

const Color colorBateriaLlena = Color(0xFF77C000); // Indicador de batería llena
const Color colorFondoInfo = Colors.blueGrey; // Fondo para información (temperatura, voltaje, etc.)
const Color colorFondoMarcadorMarcha = Color(0xFF6B4339); // Fondo del indicador de marcha

// Paleta base usada en dashboard.dart
const Color colorPrimario = Color(0xFF6B4339); // Color principal (base)
const Color colorPrimarioOscuro = Color(0xFF6B4339); // Variante oscura del color principal
const Color colorPrimarioMedio = Color(0xFF52342C); // Variante media del color principal
const Color colorPrimarioClaro = Color(0xFF8B7268); // Variante clara del color principal
const Color colorGradienteFinal = Color(0xFF7D7472); // Color de terminación del gradiente
const Color colorBrilloSecundario = Color(0xFFE2BDAD); // Brillo decorativo secundario
